chip:
  label: Chip
  description: Chips are compact elements that allow users to enter information, select a choice, filter content, or trigger an action. https://m2.material.io/components/chips
  variants:
    default:
      label: Default
    selected:
      label: Pre-selected
      description: Colored inside a 'choice' chip set.
    touch:
      label: Touch
      description: Extend the input beyond the visual bounds of the element.
  settings:
    icon:
      label: Icon
      type: textfield
      descrioption: Replaced by the checkmark on selected variant.
      preview: event
  fields:
    text:
      type: text
      label: Text
      description: The chip text
      preview: Add to calendar
  libraries:
    - chip:
        dependencies:
          - ui_suite_material/material_icons
